{
  "name": "fastapi-codespace",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "web",
  "workspaceFolder": "/app",

  "forwardPorts": [8000, 5432],

 "postCreateCommand": "cp -n .env.example .env && pip install -r requirements.txt && apt-get update && apt-get install -y nano && git config --global core.editor 'nano'",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}